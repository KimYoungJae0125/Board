<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.board.products.model.dao.ProductsDao">

    <select id="getProducts" parameterType="Products" resultType="Products">
        /* sqlId = com.board.products.model.dao.ProductsDao.getProducts */

        SELECT
        *
        FROM
        PRODUCTS
    </select>


    <select id="viewProduct" parameterType="String" resultType="Products">
        /* sqlId = com.board.products.model.dao.ViewDao.viewProduct */

        SELECT
        *
        FROM
        PRODUCTS
        WHERE
        ID = #{id}
    </select>

    <insert id="insertProduct" parameterType="Products">
        INSERT
        INTO
        PRODUCTS
        (
          NAME
        , PRICE
        )
        VALUES
        (
          #{name}
        , #{price}
        )
    </insert>

    <delete id="deleteProduct" parameterType="String">
        DELETE
        FROM
        PRODUCTS
        WHERE
        ID = #{id}
    </delete>

</mapper>
