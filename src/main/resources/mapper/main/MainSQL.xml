<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.board.common.model.dao.MainDao">

    <select id="selectList" parameterType="Products" resultType="Products">
        /* sqlId = com.board.common.dao.MainDao.selectList */

        SELECT
        *
        FROM
        PRODUCTS
    </select>
</mapper>
